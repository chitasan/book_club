<% if object.errors.any? %>
  <div id=“error_message”>
      <%= pluralize(object.errors.count, @error) %> prevented
      it from being saved:
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
 <% end %>